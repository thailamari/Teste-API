{
  "name": "api-transferencias",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "mocha test/**/*.test.js --timeout 10000 --reporter mochawesome",
    "test-external": "mocha test/external/**/*.test.js --timeout 10000",
    "test-controller": "mocha test/controller/**/*.test.js --timeout 10000",
    "test-user": "mocha test/User/**/*.test.js --timeout 10000"
  },
  "dependencies": {
    "chai": "^4.5.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mocha": "^11.7.1",
    "sinon": "^21.0.0",
    "supertest": "^7.1.4",
    "swagger-ui-express": "^4.6.3"
  },
  "devDependencies": {
    "mochawesome": "^7.1.3"
  }
}
